Place stage-specific bird sprites here.

- classic mode (level id `medium`): `classic.png`
- high volatility (level id `devil`): `high-volatility.png`

Images should be roughly 64×48 pixels (they will be scaled to the in-game bird size and rotated). If a file is missing or fails to load, the fallback vector bird will be used instead.
